{
  "name": "@versioning-demo/app",
  "version": "1.2.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "serve": "^12.0.0"
  },
  "scripts": {
    "start": "yarn build && serve public",
    "build": "cp package.json ./public/package.json"
  }
}
